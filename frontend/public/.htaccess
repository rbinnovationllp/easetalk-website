Options -MultiViews
RewriteEngine On
RewriteBase /

# Keep real files and folders working normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let React handle /admin and other app routes on Hostinger/Apache.
RewriteRule ^ index.html [L]
